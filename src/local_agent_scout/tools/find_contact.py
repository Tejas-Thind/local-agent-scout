"""Stage: surface a contact for a high-fit company.

Founders are often already in the source post. For a verified email, this is
the one step where free runs out, so defer it to the few companies that score
high and you actually want to reach. Employer tools (Harmonic) stay manual and
out of this automated path.

TODO(tejas): build late. Start with "use the founder names already extracted".
"""

from __future__ import annotations

from typing import Any


def find_contact(company: dict[str, Any]) -> dict[str, Any]:
    raise NotImplementedError
